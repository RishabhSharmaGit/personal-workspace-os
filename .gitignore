# secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Bun / Node
node_modules/
*.log
bun.lockb
# bun.lock (text lockfile, Bun >=1.2) IS committed intentionally — keep tracked.

# Supabase local
db/supabase/.temp/
db/supabase/.branches/

# Claude Code personal overrides
.claude/settings.local.json
.mcp.json

# Local indexer cache
scripts/.cache/

# Obsidian vault state (per-user, volatile)
.obsidian/
**/.obsidian/

# Personal source artifacts ingested into career-os (do not commit)
InstagramDump/
Profile.pdf

